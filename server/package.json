{
  "name": "server",
  "version": "1.0.0",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sh scripts/start.sh build/src/index.js",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "Jeremias Abreu (jeremiass.abreu@gmail.com)",
  "license": "ISC",
  "description": "A key-value store web service for circuit-breaker pattern demo",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "20.12.7",
    "gts": "^5.3.1",
    "typescript": "^5.2.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
